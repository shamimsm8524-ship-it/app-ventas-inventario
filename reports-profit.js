(()=>{
  const money=n=>'S/ '+(Number(n)||0).toFixed(2);
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const arr=n=>{try{return Array.isArray(window[n])?window[n]:eval(`typeof ${n}!=='undefined'&&Array.isArray(${n})?${n}:[]`)}catch{return[]}};
  const style=()=>{if(document.getElementById('vareliaReportsCss'))return;const s=document.createElement('style');s.id='vareliaReportsCss';s.textContent=`
  .vareliaExtraView{padding:2px 0 30px}.extraHead{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;margin-bottom:18px}.extraHead h2{margin:0;font-size:27px;letter-spacing:-.035em}.extraHead p{margin:5px 0 0;color:var(--muted);font-size:13px}.extraActions{display:flex;gap:8px;flex-wrap:wrap}.extraBtn{border:1px solid var(--line);background:var(--card);color:var(--ink);padding:10px 13px;border-radius:12px;font-weight:800;cursor:pointer}.extraBtn.active,.extraBtn.primary{background:linear-gradient(135deg,#f0066e,#a92bf5);color:#fff;border-color:transparent}.extraGrid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-bottom:14px}.extraCard{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:16px;box-shadow:0 10px 28px rgba(15,23,42,.055)}.extraCard small{display:block;color:var(--muted);font-size:11px;font-weight:800}.extraCard strong{display:block;font-size:23px;margin-top:7px;letter-spacing:-.03em}.extraPanel{background:var(--card);border:1px solid var(--line);border-radius:20px;padding:17px;box-shadow:0 10px 30px rgba(15,23,42,.055);margin-bottom:14px}.extraPanel h3{margin:0 0 13px;font-size:15px}.extraTable{width:100%;border-collapse:collapse}.extraTable th,.extraTable td{text-align:left;padding:10px 8px;border-bottom:1px solid var(--line);font-size:11px}.extraTable th{color:var(--muted);font-weight:800}.profitGood{color:#16a34a}.profitBad{color:#dc2626}.extraEmpty{padding:24px;text-align:center;color:var(--muted);font-size:12px}.extraHelpGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.extraHelpItem{padding:15px;border:1px solid var(--line);border-radius:16px;background:var(--card)}.extraHelpItem b{display:block;margin-bottom:5px}.extraHelpItem span{font-size:12px;color:var(--muted);line-height:1.45}@media(max-width:760px){.extraHead{flex-direction:column}.extraGrid{grid-template-columns:repeat(2,minmax(0,1fr))}.extraCard strong{font-size:19px}.extraActions{width:100%}.extraBtn{flex:1}.extraHelpGrid{grid-template-columns:1fr}.extraTable th:nth-child(2),.extraTable td:nth-child(2){display:none}}
  `;document.head.appendChild(s)};
  function show(id){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===id));document.querySelectorAll('#sidebar .nav button').forEach(b=>b.classList.remove('active'));const cls=id==='reports'?'.premiumReportsItem':id==='profit'?'.premiumGainItem':id==='help'?'.premiumHelpItem':null;if(cls)document.querySelector(cls)?.classList.add('active');document.getElementById('sidebar')?.classList.remove('show');document.querySelector('.overlay')?.classList.remove('show');}
  function saleCost(s,products){return (Array.isArray(s.items)?s.items:[]).reduce((sum,it)=>{const q=Number(it.qty||it.quantity||1)||1;const p=products.find(x=>String(x.id)===String(it.id||it.productId));const c=Number(it.buyPrice??it.cost??p?.buyPrice??0)||0;return sum+c*q},0)}
  function periodSales(days){const sales=arr('sales'),now=new Date();if(days==='month')return sales.filter(s=>{const d=new Date(s.date);return d.getFullYear()===now.getFullYear()&&d.getMonth()===now.getMonth()});const cutoff=new Date(now);cutoff.setDate(now.getDate()-Number(days||30));return sales.filter(s=>new Date(s.date)>=cutoff)}
  function makeReports(main){let sec=document.getElementById('reports');if(!sec){sec=document.createElement('section');sec.id='reports';sec.className='view vareliaExtraView';main.appendChild(sec)}let range='30';const render=()=>{const ss=periodSales(range),ps=arr('products'),pu=arr('purchases'),mv=arr('movements');const revenue=ss.reduce((a,s)=>a+(Number(s.total)||0),0);const units=ss.reduce((a,s)=>a+(Array.isArray(s.items)?s.items.reduce((x,i)=>x+(Number(i.qty||i.quantity||1)||1),0):0),0);const low=ps.filter(p=>(Number(p.stock)||0)<=Math.max(0,Number(p.reorderLevel)||0)).length;const purchaseTotal=pu.reduce((a,p)=>a+(Number(p.total)||0),0);const top=[...ss].sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,8);sec.innerHTML=`<div class="extraHead"><div><h2>Reportes</h2><p>Análisis general de ventas, inventario, compras y movimientos.</p></div><div class="extraActions"><button class="extraBtn ${range==='7'?'active':''}" data-r="7">7 días</button><button class="extraBtn ${range==='30'?'active':''}" data-r="30">30 días</button><button class="extraBtn ${range==='month'?'active':''}" data-r="month">Este mes</button><button class="extraBtn primary" id="exportReportCsv">Exportar CSV</button></div></div><div class="extraGrid"><div class="extraCard"><small>Ventas</small><strong>${money(revenue)}</strong></div><div class="extraCard"><small>Transacciones</small><strong>${ss.length}</strong></div><div class="extraCard"><small>Unidades vendidas</small><strong>${units}</strong></div><div class="extraCard"><small>Stock bajo</small><strong>${low}</strong></div></div><div class="extraGrid"><div class="extraCard"><small>Productos</small><strong>${ps.length}</strong></div><div class="extraCard"><small>Compras registradas</small><strong>${pu.length}</strong></div><div class="extraCard"><small>Valor de compras</small><strong>${money(purchaseTotal)}</strong></div><div class="extraCard"><small>Movimientos inventario</small><strong>${mv.length}</strong></div></div><div class="extraPanel"><h3>Ventas recientes del período</h3>${top.length?`<div style="overflow:auto"><table class="extraTable"><thead><tr><th>ID</th><th>Fecha</th><th>Total</th><th>Productos</th></tr></thead><tbody>${top.map(s=>`<tr><td>#${esc(String(s.id||'').slice(-7))}</td><td>${new Date(s.date).toLocaleString('es-PE')}</td><td>${money(s.total)}</td><td>${Array.isArray(s.items)?s.items.length:0}</td></tr>`).join('')}</tbody></table></div>`:'<div class="extraEmpty">No hay ventas en este período.</div>'}</div>`;sec.querySelectorAll('[data-r]').forEach(b=>b.onclick=()=>{range=b.dataset.r;render()});sec.querySelector('#exportReportCsv').onclick=()=>{const rows=[['ID','Fecha','Total'],...ss.map(s=>[s.id||'',s.date||'',Number(s.total)||0])];const csv=rows.map(r=>r.map(v=>`"${String(v).replace(/"/g,'""')}"`).join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='reporte-varelia.csv';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}};render();return sec}
  function makeProfit(main){let sec=document.getElementById('profit');if(!sec){sec=document.createElement('section');sec.id='profit';sec.className='view vareliaExtraView';main.appendChild(sec)}const render=()=>{const ss=periodSales('month'),ps=arr('products');const revenue=ss.reduce((a,s)=>a+(Number(s.total)||0),0);const cost=ss.reduce((a,s)=>a+saleCost(s,ps),0);const profit=revenue-cost,margin=revenue>0?profit/revenue*100:0;const by={};ss.forEach(s=>(s.items||[]).forEach(it=>{const id=String(it.id||it.productId||it.name||'');const p=ps.find(x=>String(x.id)===id);const name=it.name||p?.name||'Producto';const q=Number(it.qty||it.quantity||1)||1;const sell=Number(it.sellPrice??it.price??p?.sellPrice??0)||0;const buy=Number(it.buyPrice??it.cost??p?.buyPrice??0)||0;by[name]=(by[name]||0)+(sell-buy)*q}));const tops=Object.entries(by).sort((a,b)=>b[1]-a[1]).slice(0,8);sec.innerHTML=`<div class="extraHead"><div><h2>Ganancias</h2><p>Rentabilidad del mes calculada con ventas y costo de compra.</p></div><div class="extraActions"><button class="extraBtn" id="refreshProfit">Actualizar</button></div></div><div class="extraGrid"><div class="extraCard"><small>Ingresos del mes</small><strong>${money(revenue)}</strong></div><div class="extraCard"><small>Costo de productos</small><strong>${money(cost)}</strong></div><div class="extraCard"><small>Ganancia bruta</small><strong class="${profit>=0?'profitGood':'profitBad'}">${money(profit)}</strong></div><div class="extraCard"><small>Margen</small><strong class="${margin>=0?'profitGood':'profitBad'}">${margin.toFixed(1)}%</strong></div></div><div class="extraPanel"><h3>Detalle por producto</h3>${tops.length?`<table class="extraTable"><thead><tr><th>Producto</th><th>Ganancia</th></tr></thead><tbody>${tops.map(([n,v])=>`<tr><td>${esc(n)}</td><td class="${v>=0?'profitGood':'profitBad'}">${money(v)}</td></tr>`).join('')}</tbody></table>`:'<div class="extraEmpty">Aún no hay datos suficientes para calcular ganancias por producto.</div>'}</div>`;sec.querySelector('#refreshProfit').onclick=render};render();return sec}
  function makeHelp(main){let sec=document.getElementById('help');if(!sec){sec=document.createElement('section');sec.id='help';sec.className='view vareliaExtraView';main.appendChild(sec)}sec.innerHTML=`<div class="extraHead"><div><h2>Ayuda</h2><p>Guía rápida de cada módulo de Varelia.</p></div></div><div class="extraHelpGrid"><div class="extraHelpItem"><b>Productos</b><span>Crear, editar y organizar el catálogo.</span></div><div class="extraHelpItem"><b>Inventario</b><span>Controlar stock y movimientos.</span></div><div class="extraHelpItem"><b>Compras</b><span>Gestionar proveedores e ingreso de mercadería.</span></div><div class="extraHelpItem"><b>Ventas</b><span>Registrar ventas y consultar operaciones.</span></div><div class="extraHelpItem"><b>Caja</b><span>Controlar apertura, movimientos y cierres.</span></div><div class="extraHelpItem"><b>Reportes</b><span>Analizar el negocio en conjunto.</span></div><div class="extraHelpItem"><b>Ganancias</b><span>Revisar utilidad, margen y detalle por producto en un solo lugar.</span></div><div class="extraHelpItem"><b>Ajustes</b><span>Personalizar apariencia y preferencias.</span></div></div>`;return sec}
  function removeDuplicateProfit(){document.querySelectorAll('[data-view="profits"]').forEach(el=>el.remove());document.getElementById('profits')?.remove()}
  function init(){style();removeDuplicateProfit();const main=document.querySelector('main.content'),r=document.querySelector('.premiumReportsItem'),g=document.querySelector('.premiumGainItem'),h=document.querySelector('.premiumHelpItem');if(!main||!r||!g||!h){setTimeout(init,350);return}makeReports(main);makeProfit(main);makeHelp(main);r.onclick=e=>{e.preventDefault();show('reports');makeReports(main)};g.onclick=e=>{e.preventDefault();show('profit');makeProfit(main)};h.onclick=e=>{e.preventDefault();show('help');makeHelp(main)};r.dataset.view='reports';g.dataset.view='profit';h.dataset.view='help';window.vareliaShowExtraView=show;setTimeout(removeDuplicateProfit,1200)}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>setTimeout(init,1900)):setTimeout(init,1900);
})();